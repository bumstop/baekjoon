// .split(' ') : 입력값 구분이 공백으로 들어올 경우 
// .split('\n') : 입력값 구분이 줄바꿈으로 들어올 경우
const input1 = require('fs').readFileSync('/dev/stdin').toString().split(' '); // 백준 업로드
const input2 = require('fs').readFileSync('example.txt').toString().split('\n'); // 문제풀이

const a = input[0]; // 값이 string 인 경우
const b = input[0]; // 값이 string 인 경우

const c = parseInt(input[1]); // 값이 number 인 경우
const d = parseInt(input[1]); // 값이 number 인 경우

